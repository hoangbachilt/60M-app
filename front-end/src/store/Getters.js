export const point = (state) => {
  return state.user.point
}

export const seats = (state) => {
  return state.seats
}

export const carts = (state) => {
  return state.user.carts
}

export const counts = (state) => {
  return state.user.countCart
}
